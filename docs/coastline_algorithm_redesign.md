# 海岸线自然化算法问题分析与重构方案

> 状态：现行 ｜ 记录日期：2026-06-10 起持续更新
>
> §2 根因与 v2.1~v2.8 的问题演化链已摘成开发记录 `docs/devlog/2026-06-10-海岸线-八轮实测修正.md`（在工作区根仓库，不在本仓）。**参数、公式与实测数据以本文为准。**

> 对象：`src-tauri/src/map/coastline.rs`（入口 `build_natural_coastline_polygon`）
> 关联：`constants.rs`（默认参数）、`types.rs`（`CoastlineParams` 协议）、前端 `src/features/maps/components/WorldMapPanel.tsx`（质量档位参数下发）
> 日期：2026-06-10

## 落地状态（2026-06-10 更新）

方案 A 已实现为**独立新模块** `src-tauri/src/map/coastline_v2.rs`，与本报告的差异和补充：

- **v1 完全未动，双算法并行**（用户决策：历次重构均不如原版，保险起见保留 v1 为默认）。
  因此 §5 中"旧字段废弃"不再执行：`CoastlineParams` 原样保留并继续服务 v1，
  v2 使用独立的 `CoastlineV2Params`（`types.rs`）与 `COASTLINE_V2_*` 默认常量（`constants.rs`）。
- **选择开关**：`request.meta.ext.coastlineAlgorithm == "v2"` 时走 v2，参数从
  `request.meta.ext.coastlineV2Params` 读取；缺省走 v1，前后端可独立发布。
- **前端入口**：`WorldMapPanel.tsx` 海岸线面板新增「算法引擎」分段开关（经典 v1 / 实验 v2），
  简单模式的档位/尺度/三层扰动旋钮按 `buildCoastlineV2Params` 映射到 v2 参数。
- **顶点拍板结果**：v2 不保留原始顶点钉死（§6.1 风险已确认接受）。
- **已验证**：scratch 工程内 11 项单测全过（含顶点细分不变性 Hausdorff < 0.5px）；
  位移统计显示峰值打满振幅上限、长短边悬殊形状质感一致（mean 5.40 vs 5.41px）。
  注意 `app_main` 本地 `cargo test` 因 STATUS_ENTRYPOINT_NOT_FOUND 无法直接运行（既有坑，见 AGENTS.md 第 6 节）；
  公开 API 回归放在 `src-tauri/tests/coastline_v2.rs` 集成测试（经清单注入可直接运行）。
- **待办**：`tauri:dev` 下的人工视觉验收（对照 §6.2 的三个用例）。

### v2.1 修正：波长带改绝对像素单位（2026-06-10 实测反馈后）

首版 v2 把谐波带定义为"每周长周期数"，实测暴露两个问题：小岛的细节带波长被压缩成
~4px 锯齿、大陆只剩长波（**跨形状的周长依赖**——形状内的边长依赖修掉了，但换了个尺度回来）；
三带 RMS 归一 + 权重混合 + 整体峰值归一把 B/C 带挤成 A 带上的毛刺（**三层不可见**）。修正：

- **波长/振幅全部改为绝对像素**：A 带（宏观位移）360~900px/±28px、B 带（波动）90~220px/±9px、
  C 带（细节）22~55px/±3px；每个形状按自身周长换算整数谐波，容不下宏观波长的小岛自动跳过该带，
  并带平滑淡入（P 达到波长下限 1.5 倍时满幅）避免相邻尺寸外观突变。
- **每带独立峰值归一后按绝对振幅叠加**，三层互不挤压；小图形另有 0.045/0.018/0.008×周长护栏。
- **角点帐篷上限**：位移 ≤ 离角点弧距 × 0.8·tan(内角/2)（凹角再乘 0.45）——绝对振幅变大后，
  仅靠衰减窗口不足以阻止尖角两侧沿不同法线位移后折叠（实测尖三角曾因此连环自交坍缩到回退分支）。
- 采样步长 = 最小波长 / 4（自动满足 Nyquist），质量档位只控制采样点数上限（256~2048）。
- 实测位移统计：周长 2700/1000/760 的三个形状 mean 均为 10~12px、max ~30px（质感一致）；
  周长 360/140 的小岛按护栏降为 mean 2.6/1.3px，无锯齿。

### v2.2 修正：相对尺度 + 角点圆化（2026-06-10 二次实测反馈后）

v2.1 的绝对像素带在渲染验收中暴露三个问题：扰动强度太低不像海岸线；
原始顶点处被"角点帐篷"钉死形成 ~10° 针刺，不是自然地形；微型岛几乎退化回原始矩形
（绝对振幅 × 周长比例护栏对小图形切得太狠）。修正：

- **波长/振幅改为"相对周长 + 绝对界限"**（分形自相似）：波长 = 周长/divisor
  （A 带 P/7~P/2.5，B 带 P/18~P/7.5 下限 40~90px，C 带 P/50~P/22 下限 16~36px）；
  振幅 = 周长 × 比例（A 0.03 / B 0.012 / C 0.005），封顶 50/16/6px。
  小岛获得与大陆相同的"视觉性格"（位移/周长 ≈ 2~4% 各尺寸一致），大陆不会无限狂野。
- **角点圆化替代角点帐篷**：加噪声前对重采样基线做 Laplacian 扩散
  （直线段不动，只磨角，半径默认 22px、0.06×周长封顶）——自然地形没有精确尖角；
  帐篷机制删除，针刺消失。凸角不再衰减（岬角获得完整噪声），仅凹角保留温和衰减。
- **安全约束升级为"弧距/空间距比值"判据**：对面轮廓空间距离 < 弧长距离 × 0.5 才视为
  窄缝/尖角/细颈并限幅，否则视为轮廓自然延伸跳过。该判据天然保证顶点细分不变性
  （共线细分边 d ≈ 弧距），并自动保护尖角（楔形两侧 d ≈ 弧距 × sin 角度）——
  替代了帐篷的安全职责且不钉死任何点。
- 合计振幅画布护栏放宽至 0.12×短边（纯防失控）。
- 实测：周长 2700/1000/360/140 位移 max 60.6/35.6/13.9/5.0px，相对比例一致；
  尖三角不再坍缩（此前帐篷删除后曾因尖角折叠连环自交，由比值判据接住）。

### v2.3 修正：混合谱——细节带改绝对波长（2026-06-11 三次实测反馈后）

v2.2 三带全相对后，大图形的细节带波长被拉长（C 带 = P/50~P/22，大陆上 54~123px）
且振幅被绝对上限压住，导致"大形状的长直边每像素粗糙度低于小岛"——用户观察为
"长边比短边平滑"。用户提议的"预细分等长多边形"无效：算法第一步本就是 4~8px 等弧长
重采样，`v2_vertex_insertion_invariance` 测试证明插入等分点输出不变（< 0.5px）。
真因是波长尺度策略，修法为**混合谱**：

- **A 带（宏观）保持相对**：λ = P/7~P/2.5——大海湾随陆块缩放（分形直觉正确的部分）。
- **C 带（细节）改绝对**：λ = 18~45px 固定——任何形状、任何边上每像素粗糙度一致，
  这与真实地图一致：固定渲染比例下细节质感处处相同。采样步长随之全图统一（4.5px）。
- **B 带（波动）半相对**：λ = P/18~P/7.5 夹在绝对窗口 [40,110]~[90,240]px 内。
- 振幅微调：A 0.03P 封顶 55px、B 0.013P 封顶 20px、C 0.012P 封顶 4px
  （C 带波长变短后 4px 振幅的视觉粗糙度高于旧 6px@54~123px）。
- 实测：周长 2700/1000/360/140 位移 max 70.5/34.4/15.0/6.2px；
  大陆长直边与小岛的细节纹理（18~45px 波长）密度一致。

### v2.4 调整：默认风格定"硬朗"档 + 软饱和限幅（2026-06-11 app 实测对比后）

用户在 app 中对比 v1/v2 默认效果后拍板两项（详见对比渲染：同一形状同一 seed 的
柔和/硬朗/峡湾三档）：

- **默认口味烘焙为"硬朗"档**：B 带振幅 0.013P/20px → 0.018P/28px（×1.4），
  C 带 0.012P/4px → 0.024P/8px（×2）。三个扰动滑杆保持中性 1.0，仍可在 0~2 范围微调。
- **硬截断改软饱和**（`soft_limit_offset`）：位移在安全上限 60% 以内原样通过，
  超过后 tanh 平滑渐近逼近上限（严格 < 上限，安全语义不变）。修复实测截图中
  细长尾巴两侧同时贴在限幅墙上形成的"平行壁/细针"伪影与单点毛刺。
  细尾形状回归：尾部间距 13~26px 有机起伏，无自交。

### v2.5 增强：局部特征尺寸限幅（2026-06-11 编辑器叠加对比后）

用户在编辑器中叠加草稿与结果，发现偏离幅度（±~90px 峰值）让结果"认不出是画的形状"。
经三档贴合度对比（全局砍宏观带 vs 局部方案），用户拍板做**局部特征尺寸方案**：

- 每个采样点先测**局部肢体宽度** `local_feature_size`：复用折叠几何判据
  （对面轮廓中空间距离 < 0.5×弧距的最近距离），宽阔腹地为 ∞。
- **分带限幅**：各带本地振幅 = min(全局振幅, 特征比例 × 肢体宽度)，
  比例 A 0.2 / B 0.32 / C 0.5——宏观海湾需要大空间才施展，细窄肢体上自动熄火，
  但细节纹理几乎不受限（这是比"对合成位移整体限幅"更好的关键：质感不被一起压扁）。
- `build_noise_offsets` 重构为 `build_noise_bands` 逐带返回；
  原 `max_safe_offset` 拆分为 `local_feature_size` + `location_safe_cap`，
  安全上限（0.38×宽度 软饱和）语义不变。
- 回归：同周长 1760 下，80px 宽细长条偏离 max 34.5px（宏观熄火、贴草稿），
  440px 方块 max 66px（腹地大湾保留）——限幅是局部的而非全局一刀切。
  新增集成测试 `v2_thin_limb_keeps_identity_while_wide_body_stays_bold`。
- 注：`cargo test` 在 app 运行时会因主 exe 被锁失败，
  可用 `CARGO_TARGET_DIR=target-test cargo test --test coastline_v2` 绕开。

### v2.6 增强：质量档位驱动细节精细度（2026-06-11 印刷档反馈后）

用户反馈印刷档只有 1600+ 点（对比 v1 实测 10000+ 点 <1s）。澄清：点数本身不是细节，
v1 的点距远小于其最小波长，多数点是浪费；v2 点数 = 最细波长 ÷4 的"刚好够用"密度。
正确的放开方式是让质量档位控制**细节波长精细度**，点数随之自然上涨：

- 新参数 `detailWavelengthScale`（默认 1.0，范围 0.25~2.0）：缩放 C 带波长与采样步长。
  档位映射：预览 1.5/512 点上限 → 均衡 1.0/2048 → 精细 0.7/4096 → 印刷 0.5/10240
  （印刷档细节波长 9~22.5px、步长 2.25px）。点数硬上限 2048 → 12288。
- **谐波分层抽样**：波长精细 + 周长大时整数谐波可达数千，全量求和是
  O(谐波×采样点) 的性能黑洞；每带谐波数封顶 96，超限时分层抽样
  （每层 hash 抖动取一条，谱形/确定性/闭合性保持；带窄时退化为全量，向后兼容）。
- 性能基准（opt-level 2）：周长 2400 印刷档 1065 点 4ms；周长 7200（典型大陆）
  3194 点 30ms；周长 19000 巨图 8517 点 188ms，全部无自交。
  注意 tauri dev 是 debug 构建，巨图印刷档可能慢 5~10 倍；release 无虞。

### v2.7 修正：消除机械毛刺——自相似缩放 + 粗糙度调制（2026-06-11 印刷档实测后）

新后端印刷档（3200+ 点）实测"毛刺感太重，需要随机一些"。两个具体来源与修法：

- **细节振幅随波长自相似缩放**：印刷档把 C 带波长砍半但振幅原样（8px 压在 9~22px 上），
  坡度翻倍呈机械毛刺。改为振幅 × detail_scale（≤1 方向），保持坡度自相似——
  细节更细腻而非更尖锐。
- **粗糙度调制（间歇性）**：C 带原先全轮廓恒定振幅，窄带噪声 + 均匀包络 = 机器纹。
  新增低频粗糙度包络（波长 P/8~P/2，独立盐值，归一 [0,1]）沿轮廓调制振幅：
  C 带深度 0.7、B 带 0.35、A 带不调制——礁石段粗糙、滩涂段平静。
- **带内谱线随机加大**：谐波振幅随机区间从 [0.5,1] 放宽到 [0.25,1]
  （`COASTLINE_V2_HARMONIC_RANDOM_FLOOR`），纹理更不规则。

### v2.8 增强：结构层软封顶（压限），控制贴合度（2026-06-11 偏离过大反馈后）

用户观察"偏离过大，尤其大波动叠中波动处离草稿太远，认不出形状"。诊断：三带绝对振幅
相加（A55+B28+C8=91px 峰值），**只在 A、B 同相同向的少数岸段冲到尖峰**——是偏离
分布的**尾部**问题，不是均值。降全局振幅会治均值、连带砍掉质感；正解是 cap 尾部。

- **结构/细节分离**：A+B 归"结构层"（决定整体轮廓），C 归"细节层"。
  只对结构层合计**软封顶**到 `D_max`（`soft_limit_offset`，tanh，非硬截断）；
  C 始终全振幅叠加 → 封顶区仍有细碎起伏，**不出平直边**（区别于硬 clamp 的平行壁）。
- D_max 默认 35px（绝对，紧端约 15px），再与 `0.3×局部特征尺寸`取小（窄处更贴）。
  小图形的 per-band feature_ratio 已先压一道，故 D_max 主要在开阔大陆上起作用——
  正是偏离问题最严重处。
- 末端安全限幅（防自交 0.38×特征尺寸 / 关键点）保留，与 D_max 各司其职。
- 协议新增 `max_deviation` / `maxDeviation`（默认走常量）；未来由"比例尺"驱动。
- 实测（周长 ~2400 开阔大陆）：D_max 关→默认35→紧15，偏离 max 67→约38→约26，
  而 **mean 几乎不变**（尾部被削、整体质感保留），三档均无自交。
  新增集成测试 `v2_structural_cap_trims_deviation_without_breaking_shape`。

---

## 0. 该不该做（前提判断）

**该做，且只能重构、不能调参。** 理由：用户观察到的两个症状（包络抹杀噪声、效果随原始边长漂移）不是参数没调好，而是同一个结构性根因的两个表现——**噪声场以"边"为单位参数化**（`t ∈ [0,1]` 逐边、相位逐边随机）。这个设计下：

- 包络 `sin(πt)^1.15` 是**结构必需品**：噪声在每条边的 `t=0/1` 处相位不连续，去掉包络就会在原始顶点处产生跳变和折角。包络不是可以删掉的装饰，它是给逐边噪声"擦屁股"的。
- 所以"把包络调弱/指数调小"救不了：包络弱了顶点处接缝就露出来；包络强了噪声就被压扁。这是一对死锁，调参只能在两个坏结果之间滑动。

**最强反对意见**：MVP 阶段是否值得为视觉效果投入一次算法重写？这是价值判断，不在本报告管辖范围。本报告只论证：(a) 现状缺陷是结构性的；(b) 重构成本可控（核心改动集中在 `naturalize_vertices` 一个函数 + 一个新噪声模块，验证/回退管线全部保留）。

**可证伪条件**：如果实际产品中用户绘制的草稿顶点非常密（顶点间距已接近期望的海岸线特征波长，比如自由手绘连续采样），那么"逐边"和"全局"参数化的差异会自然消失，本报告主张的重构收益就不成立。从现有测试样例（4～8 个稀疏顶点）和 MVP 文档看，草稿是稀疏多边形，前提成立。

---

## 1. 现状算法流程（速览）

```text
原始多边形（n 个顶点）
  └─ 逐边处理（for edge_index in 0..n）：
       ├─ 细分数 = f(边长, 周长占比, 归一化边长)        ← 边长决定
       ├─ 振幅   = clamp(边长 × base × √归一化, 2, 0.025×画布)  ← 边长决定
       ├─ 对每个细分点 t：
       │    offset = 振幅 × sin(πt)^1.15 × 三层正弦噪声(edge_index, t)
       │    offset 再被安全限幅（0.35×边长、0.75×角距、凹角/尖角系数、
       │                         最近非邻边距离×0.38、关键点距离×0.5）
       │    沿"该边固定法线"位移
  └─ 去重 → Laplacian 平滑(2 遍, w=0.16) → 自交/关键点校验
  └─ 失败则按 0.75/0.55/0.40 降扰重试 → 再失败回退纯平滑 → 再失败返回原轮廓
```

---

## 2. 根因分析

### 2.1 包络为什么"抹杀"了三层噪声

`envelope = sin(πt)^1.15` 在每条边两端强制归零，量化看：

| 位置（占边长） | 包络保留的振幅 |
|---|---|
| 距顶点 5% | ≈ 12% |
| 距顶点 10% | ≈ 26% |
| 距顶点 20% | ≈ 54% |

即**每条边有 40% 的区段（两端各 20%）噪声振幅被压到 54% 以下**，顶点本身被钉死为零位移。后果有三层：

1. **包络自己成了最显眼的"形状"。** A 层噪声频率为每边 1.0～4.5 个周期，和包络（半个周期的正弦鼓包）处于同一尺度。两者相乘后，视觉上占主导的是"每条边一个对称鼓包"，三层噪声退化为鼓包表面的小涟漪——这正是"被包络抹杀"的数学解释。
2. **所有原始顶点是位移零点 → 扇贝状外观。** 轮廓呈现"一边一瓣"的花瓣/扇贝模式，周期性与原始顶点严格对齐，人眼立刻识别出底层多边形。
3. **噪声特征永远无法跨越顶点。** 噪声场被原始顶点切割成 n 个互不连通的"格子"，任何海湾/半岛特征都被困在单条边内，不可能出现横跨两条边的大尺度地貌。

### 2.2 "效果因原始边长不同而不同"的四个来源

1. **空间频率 ∝ 边长。** 波频单位是"每边周期数"：同样 2 个周期，400px 的边产生波长 200px 的起伏，40px 的边产生波长 20px 的起伏。同一形状内长短边的海岸"质感"（粗糙度的空间尺度）完全不同。
2. **振幅与限幅 ∝ 边长。** 名义振幅 = `边长 × 1.0 × √归一化`，再被 `0.025×画布` 截断。实算下来：画布 1000×640 时上限 16px，**任何边长 > ~16px 的边名义振幅都顶满上限**，看似恒定；但随后 `constrain_coastline_offset` 的 `0.35×边长` 和三角帐篷 `0.75×边长×min(t,1-t)` 对短边（< ~45px）全程生效——短边实际是被"边长比例限幅"统治的，长边是被"画布上限"统治的。两种统治方式不同 → 长短边行为分裂。
3. **细分预算 ∝ 边长。** 段数公式含 `归一化边长` 与 `周长占比` 两个边长项。
4. **位移方向逐边固定。** 整条边的所有细分点沿同一条法线位移，相邻边法线方向跳变，靠包络归零来掩盖折痕——又回到 2.1 的死锁。

### 2.3 附带缺陷（重构时应一并处理）

- **C 层噪声欠采样（接近甚至低于 Nyquist）。** C 层最高 11.6 周期/边，而典型细分只有 17～32 段，即每周期仅 1.5～4.9 个采样点。每周期 < 2 点时纯粹是混叠伪影，根本画不出第三层细节。
- **Laplacian 平滑随后把残存高频磨光。** w=0.16 两遍后，每周期 3 点的成分只剩 ~27%，每周期 2.2 点的只剩 ~14%。**"三层噪声"实际只有 A 层和部分 B 层活到了输出**，C 层先混叠再被磨平。
- **Laplacian 平滑有收缩效应**，轮廓整体向内缩，加剧"被限制在原始边内"的观感。
- **安全降扰是全局连坐。** 一处自交触发整条轮廓 0.75/0.55/0.40 的全局降幅，问题点和无辜区段一起变平。

---

## 3. 候选方案对比

| | 方案 A：周长域噪声（推荐） | 方案 B：中点位移分形细分 | 方案 C：2D 域噪声场 |
|---|---|---|---|
| 思路 | 全轮廓按弧长 s∈[0,P) 统一参数化，构造周期连续的多倍频噪声 f(s)，沿平滑法线位移 | 递归细分每条边，中点沿法线随机位移，振幅随深度衰减 | 在画布平面上定义 fBm 噪声场 f(x,y)，轮廓点按所在位置取值位移 |
| 消除顶点钉死 | ✅ 噪声连续跨越顶点，无需包络 | ❌ 原顶点仍是不动点 | ✅ |
| 消除边长依赖 | ✅ 频率/振幅以世界单位（周长比例/像素）定义 | ⚠️ 初始振幅仍逐边，需额外归一 | ✅ |
| 闭合无缝 | ✅ 整数谐波天然周期闭合 | ✅ | ✅ |
| 新增依赖 | 无（沿用现有 hash 确定性随机） | 无 | 需 noise crate 或自写 2D value noise |
| 与现有管线兼容 | 高：constrain/校验/降扰/回退全保留 | 中：递归结构与现有逐点限幅不合 | 中 |
| 风险 | 顶点不再精确保留（见 §6 风险） | 改善有限，2.1 问题只解决一半 | 法向位移仍需弧长重采样，等于 A + 额外依赖 |

**推荐方案 A**。理由：同时根治两个症状；不引入依赖；保留 `shape.id+name` 哈希确定性（同一形状每次生成结果一致，符合 MVP 文档的确定性预期）；改动半径最小。方案 C 本质是 A 的 2D 版本，多付一个依赖却没多换来什么；方案 B 保留顶点钉死，只值得作为"必须精确过原始顶点"需求出现时的备选。

---

## 4. 推荐方案 A 详细设计

### 4.1 管线

```text
原始多边形
  ① 等弧长重采样：步长 ds = clamp(P / target_points, ds_min, ds_max)
     —— 采样密度与原始顶点分布完全解耦（顶点细分不变性的基础）
  ② 周期噪声场 f(s)：三个倍频带的整数谐波叠加（见 4.2）
  ③ 局部衰减 g(s)：仅在凹角/尖角的弧长窗口内衰减（替代全边包络，见 4.3）
  ④ 平滑法线 n(s)：中心差分切线旋转 90°（替代逐边固定法线）
  ⑤ 位移：p(s) = q(s) + n(s) × clamp(A × g(s) × f(s), ±max_offset(s))
     —— max_offset 沿用现有安全限幅，但基准从"边长"换成"局部特征尺寸"（见 4.4）
  ⑥ Taubin 平滑（λ/μ 双步，无收缩）替代 Laplacian
  ⑦ 校验/降扰重试/回退管线 —— 原样保留
```

### 4.2 周期噪声场（无依赖、确定性、天然闭合）

对闭合轮廓，最简单且严格无缝的构造是**整数谐波谱合成**：

```text
f(s) = Σ_octave w_o · f_o(s)
f_o(s) = Σ_{k ∈ K_o} a_k · sin(2πk·s/P + φ_k)
```

- `k` 取整数 → `f(0) = f(P)` 严格成立，闭合无缝，不需要任何接缝处理。
- 三个倍频带以**整周周期数**定义（替代现在的"每边周期数"）：
  - A 带（宏观海湾/半岛）：k ∈ [2, 6]
  - B 带（中尺度）：k ∈ [9, 18]
  - C 带（细碎纹理）：k ∈ [28, 56]
- `φ_k`、`a_k` 由现有 `hash_unit(seed ^ salt_o ^ k)` 派生，确定性保持；`a_k ∝ k^(-β/2)`（β ≈ 1.6~2.0，分形海岸线的典型谱斜率），每个带做 RMS 归一后按 `w_o` 加权。
- 谐波数 ~30 个，每采样点 O(30) 次 sin，512 个采样点共 ~1.5 万次三角函数，微秒级，无性能问题。

**采样密度与 C 带匹配**：`target_points` 取 ≥ 4 × max(K_C)（如 k_max=56 → ≥ 224 点，默认 256～512），保证最高频带每周期 ≥ 4 个采样点，根治 2.3 的欠采样。

### 4.3 角点衰减（替代全边包络）

包络原本要解决的真问题只有一个：凹角/窄缝处大位移容易自交。新方案改为**外科手术式**处理：

- 对每个原始顶点计算现有的 `vertex_corner_offset_factor`（尖角/凹角判定逻辑保留）。
- 仅在该顶点两侧弧长 `r_c` 窗口内（`r_c ≈ 1.5 × P/k_min(C带)`，并以相邻边长一半为上限）用 smoothstep 把振幅从 `corner_factor` 过渡回 1.0。
- 凸尖角可以**不衰减或轻微衰减**——尖角外凸形成海岬反而自然；凹角维持现有 `0.45` 强衰减。

效果：衰减只发生在真正危险的角点邻域（占周长的小比例），其余 80%+ 轮廓上三层噪声全振幅工作。

### 4.4 安全限幅基准从"边长"换成"局部特征尺寸"

`constrain_coastline_offset` 的框架保留，但：

- 删除 `0.35×边长`、`0.75×角距三角帐篷`（它们是边长依赖的主要来源，且与新的角点窗口衰减重复）。
- 保留并升级：`最近非邻边距离 × 0.38`（防窄缝自交，这是真正干活的约束）、`关键点距离 × 0.5`（防关键点出界）、全局振幅上限 `A`。
- "非邻边"判定从"边索引相邻"改为"弧长距离 < 阈值"（重采样后没有原始边索引概念）。
- 复杂度仍是 O(n·m)（n 原始边数、m 采样点数），与现状同阶；编辑器尺度无压力，将来可加网格加速。

### 4.5 全局振幅

```text
A = clamp(amplitude_perimeter_ratio × P, amplitude_min, amplitude_canvas_ratio_max × 画布短边)
```

单一全局值，整条轮廓质感一致。`amplitude_perimeter_ratio` 默认 ≈ 0.02（可从现状的视觉效果反推校准）。

### 4.6 平滑

Taubin 平滑（λ=0.33, μ=-0.34，1~2 遍）替代 Laplacian：去高频毛刺但不收缩。由于 4.2 已保证采样密度，平滑需求本身也大幅下降，甚至可以默认 0 遍、仅在降扰重试分支启用。

---

## 5. 协议兼容与参数迁移

`CoastlineParams` 经 `request.meta.ext.coastlineParams` 由前端下发（`WorldMapPanel.tsx` 按质量档位传参），属于 API 模型改动，按 AGENTS.md 要求附兼容说明：

| 旧字段 | 处置 | 新字段 |
|---|---|---|
| `segmentBase/LengthFactor/EdgeRatioFactor`、`min/maxSegments`、`normalizedLength*` | 废弃（逐边细分概念消失），收到时记 warn 日志并忽略 | `targetPoints` 或 `resampleStepRatio` |
| `waveXBase/Span`（周期/边） | 废弃——无法静态映射（依赖边数） | `bandXHarmonicMin/Max`（周期/整周） |
| `amplitudeBase`（×边长） | 废弃 | `amplitudePerimeterRatio`（×周长） |
| `amplitudeMin`、`amplitudeCanvasRatioMax` | 原样保留 | — |
| `waveXWeight/Strength` | 语义不变，保留（映射到带权重） | — |
| `relaxPasses/Weight`、`fallback*`、`deduplicate*` | 保留（平滑实现换 Taubin，权重语义近似） | — |
| `noiseSalt*`、`hash*` | 原样保留 | — |
| `spectralBeta`（新增，谱斜率） | — | 默认 1.8 |

- 前端 `WorldMapPanel.tsx` 的四档质量参数需同步改为新字段（档位差异主要落在 `targetPoints` 与各带谐波上限）。
- 后端对旧字段**容忍但忽略**（serde 字段都是 `Option`，天然向后兼容），过渡期前后端可不同步发布。

---

## 6. 风险与未决问题

1. **原始顶点不再被精确保留。** 现状算法顶点钉死（位移为 0），新算法顶点同样会被噪声位移。如果产品语义是"用户画的点必须出现在最终轮廓上"，方案 A 构成回归——**需要产品确认**。若需要折中，可对原始顶点加小半径的弧长窗口衰减（同 4.3 机制），把位移压到 ~30% 而非归零。
2. **视觉回归无自动判据。** "更自然"无法单测断言，需人工对照新旧渲染图验收（建议截取等边方形、长短边悬殊多边形、窄湾形状三个用例）。
3. **极小/极细形状**（周长 < ~100px 或宽度 < 振幅量级）下整数谐波带可能拥挤，需要按周长缩减 C 带上限（`k_max ≤ P / (4·ds_min)`），实现时处理。

---

## 7. 实施步骤与验证计划

小步提交，每步独立可验证：

1. **新增纯函数模块**：`resample_polygon_by_arc_length` + `periodic_harmonic_noise`（含单测：闭合性 `f(0)=f(P)`、确定性、RMS 归一）。
2. **重写 `naturalize_vertices`** 为弧长管线（①～⑤），`constrain` 换基准；保留 `build_natural_coastline_polygon` 的校验/降扰/回退结构不动。
3. **Taubin 平滑**替换 `relax_polygon` 的调用点（函数保留给回退分支用）。
4. **参数结构扩展**（`types.rs` + `constants.rs` 新默认值），旧字段废弃告警。
5. **前端档位参数迁移**（`WorldMapPanel.tsx`、`api/map.ts`）。
6. **文档同步**：更新 `map_shape_editor_backend_mvp.md` 海岸线一节。

回归测试（新增）：

- **顶点细分不变性**（本次重构的核心判据）：对同一矩形分别用 4 顶点和插入共线点后的 16 顶点版本生成，输出轮廓的 Hausdorff 距离应小于阈值。旧算法此测试必然大幅失败，新算法应通过——这同时是"边长依赖已根治"的可证伪检验。
- 质感一致性：长短边悬殊多边形上，按弧长统计位移的局部 RMS，长边段与短边段之比应接近 1。
- 既有不变量：自交为零、关键点包含、确定性（同 seed 同输出）、窄形状安全（沿用现有窄湾用例）。

验证命令（按 AGENTS.md）：

- `src-tauri`：`cargo check` / `cargo test`（注意：`cargo test` 可能因 common-controls v6 清单缺失报 `STATUS_ENTRYPOINT_NOT_FOUND`，与本次改动无关，`cargo clean` 无效）。
- 前端改动后：`npm run lint && npm run build`。

---

## 8. 结论

| 症状 | 根因 | 对策 |
|---|---|---|
| 包络抹杀三层噪声 | 逐边噪声参数化迫使包络存在；包络与 A 层同尺度，自己成了主形状 | 全周长弧长参数化 + 整数谐波周期噪声，包络废除，角点改窗口衰减 |
| 效果随原始边长漂移 | 频率/振幅/细分/限幅四项全部以边长为基准 | 全部换成世界单位（周长比例、像素、局部特征尺寸） |
| （附带）第三层噪声不可见 | 欠采样 + Laplacian 磨平 | 采样密度对齐最高频带 + Taubin 无收缩平滑 |

**什么证据会推翻本方案**：(a) 产品要求原始顶点精确保留（→ 改用 §6.1 折中或方案 B）；(b) 实际草稿顶点密集到顶点间距 ≈ 特征波长（→ 重构收益消失）；(c) "顶点细分不变性"测试在新实现上仍不通过（→ 说明边长依赖另有未识别来源，需回到分析）。
